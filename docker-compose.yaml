services:
  poe-proxy:
    build: .
    container_name: poe-proxy
    environment:
      - POE_TOKEN=jru7WgP-PnYAaWKgiKlM24Y2MJqIrkl3A0zO-daH7bk #0n_4lHAaJynbryLZaSKSRmQZOE65dzTNubh-XwqbfRA  jru7WgP-PnYAaWKgiKlM24Y2MJqIrkl3A0zO-daH7bk
    ports:
      - "0.0.0.0:9876:8080"
    restart: unless-stopped

  poe-proxy-tests: # New service for running tests
    build: . # Uses the same Dockerfile
    container_name: poe-proxy-test-runner
    environment:
      - POE_TOKEN=jru7WgP-PnYAaWKgiKlM24Y2MJqIrkl3A0zO-daH7bk
    volumes:
      - ./:/app # Mount current directory to /app so code changes are reflected
    command: ["pytest", "-v", "tests/"]
